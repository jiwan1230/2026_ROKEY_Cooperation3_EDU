# CameraPreviewPanel.jsx가 MSI2의 web_video_server MJPEG 스트림을 직접 가리키는
# URL을 만드는 데 쓴다 - 값을 바꾸려면 이 파일을 .env.local로 복사해서 수정하세요
# (.env.local은 gitignore됨, 커밋되는 이 예시 파일은 건드리지 마세요).
VITE_MSI2_HOST=10.10.0.2
VITE_MSI2_CAMERA_PORT=8080
